import styled from "@emotion/styled";
import { DefaultButton } from "../common/DefaultButton";

export default function SwitchTeamButton() {
  return <SwitchButton>Switch</SwitchButton>;
}

const SwitchButton = styled(DefaultButton)`
  width: 5rem;
`;
